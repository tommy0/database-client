program Project1;

uses
  Forms,
  Unit1 in 'Unit1.pas' {iClient},
  DataMudule_1 in 'DataMudule_1.pas' {DataModule1: TDataModule},
  Unit2 in 'Unit2.pas' {fEditor};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TiClient, iClient);
  Application.CreateForm(TDataModule1, DataModule1);
  Application.CreateForm(TfEditor, fEditor);
  Application.Run;
end.
